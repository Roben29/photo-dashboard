import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Asset Forge — Creative Asset & 3D Model Hub",
  description:
    "A browser-based asset library for 3D designers, video editors, and prompt engineers. Upload, catalog, inspect, and organize heavy 3D assets and ultra-high-resolution images with full creation metadata.",
  keywords: [
    "3D assets",
    "asset management",
    "3D model viewer",
    "prompt registry",
    "creative pipeline",
    "Three.js",
  ],
  authors: [{ name: "Asset Forge" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
